# ==============================================================================
#
#                           LOBBY PARKOUR - BY CRUMB
#
# This config supports PlaceholderAPI, MiniMessage, and legacy color codes such
# as &d, §d, and &#ff55ff. Custom placeholders
# are written BEFORE the field where they can be used.
#
# NOTE: PlaceholderAPI is not supported in all settings at the moment.
#
# License: MIT License
#          Copyright (c) 2025 Crumb Network
#          See LICENSE file or https://opensource.org/licenses/MIT for details.
#
# ==============================================================================

disable-holograms: false # If enabled, holograms will not be spawned. This will fix issues with plugins like DecentHolograms, but it will also disable all holograms for the parkour.

# The items given to the player when starting a parkour
items:
  last-checkpoint:
    item: "minecraft:heavy_weighted_pressure_plate"
    amount: 1
    name: "<green>Last Checkpoint"
  reset:
    item: "minecraft:oak_door"
    amount: 1
    name: "<red>Reset"
  leave:
    item: "minecraft:red_bed"
    amount: 1
    name: "<red>Leave"

formatting:
  # %parkour_name% -> The name of the parkour
  start-plate: "<green>⚑</green> %parkour_name%"
  end-plate: "<red>⚑</red> %parkour_name%"
  # %checkpoint% -> The number of the checkpoint
  # %checkpoint_total% -> The total number of checkpoints (excluding start and end)
  checkpoint-plate: "<blue>⚑</blue> <gray>(%checkpoint%/%checkpoint_total%) %parkour_name%"

  # %m% -> Minutes
  # %s% -> Seconds
  # %ms% -> Milliseconds
  timer: "%m%:%s%:%ms%"

  # %parkour_name% -> The name of the parkour
  # %player_name% -> The name of the player
  start-message: "<color:#ff9c40>☄</color> <color:#474747>»</color> <color:#ffed8a>You have started <white>%parkour_name%</white>. Good Luck!</color>"
  cancel-message: "<color:#ff9c40>☄</color> <color:#474747>»</color> <color:#ffed8a>You have cancelled your run. Retry next time!</color>"
  # %timer% -> The current timer (formatted previously)
  end-message: "<color:#ff9c40>☄</color> <color:#474747>»</color> <color:#ffed8a>You have finished the parkour in <white>%timer%</white>. <color:#ffc157><b>ɢɢ!</b></color></color>"
  reset-message: "<color:#ff9c40>☄</color> <color:#474747>»</color> <color:#ffed8a>You have reset your run for <white>%parkour_name%</white>. Good luck!</color>"
  # %checkpoint% -> The number of the checkpoint
  tp-message: "<color:#ff9c40>☄</color> <color:#474747>»</color> <color:#ffed8a>You have been teleported to your last checkpoint.</color>"
  checkpoint-message: "<color:#ff9c40>☄</color> <color:#474747>»</color> <color:#ffed8a>You have reached <white>Checkpoint #%checkpoint%</white> in <white>%timer%</white>.</color>"
  # No placeholders supported
  checkpoint-skip-message: "<color:#ff9c40>☄</color> <color:#474747>»</color> <color:#ffed8a>You've missed a checkpoint!"

  flight-cancel-message: "<red>You have ended your parkour session by flying.</red>"
  flight-start-cancel-message: "<red>You cannot start a parkour while flying.</red>"

  # The action bar will be displayed while a player is doing a parkour.
  # %parkour_name% -> The name of the parkour
  # %player_name% -> The name of the player
  # %timer% -> The current timer (formatted previously)
  # %checkpoint% -> The number of the checkpoint
  # %checkpoint_total% -> The total number of checkpoints (excluding start and end)
  action-bar: "<color:#7ae0ff>%timer%</color> <color:#39aacc>⌚</color>   <dark_gray>|</dark_gray>   <color:#54ff7f><color:#57ff65>%checkpoint%</color></color><color:#b8b8b8>/%checkpoint_total%</color> <green>⚑</green>"

  leaderboard:
    # %parkour_name% -> The name of the parkour
    title: "<yellow><bold>%parkour_name%"

    # The display item will be shown above the title if enabled. (It will rotate like a dropped item.)
    display-item:
      enabled: true
      item: "minecraft:clock"
      enchant-glint: false
      spin-enabled: true

    maximum-displayed: 10
    # The personal best will be displayed below the leaderboard
    personal-best-enabled: false

    # %position% -> The score position on the leaderboard (does not include #)
    # %player_name% -> The name of the player
    # %timer% -> The completion timer (formatted previously)
    default-line-style: "#%position% - %player_name%: %timer%"
    personal-best-style: ""

    empty-line-style: "-"

    # ADVANCED - This setting will overwrite the default-line setting for a specific line (keep the line empty if you don't want to change the line.)
    lines:
      -
      -
      -

settings:
  # If enabled, the player will be teleported to the last checkpoint x,y,z + the pitch and yaw when he first activated that checkpoint
  last-checkpoint-rotation: true
  allow-fly: false
